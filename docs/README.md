## Mermaid diagram preview (offline-friendly)

Open `docs/mermaid-preview.html` in any browser to see the Mermaid code from the thesis rendered as diagrams.

Notes:

- The preview loads Mermaid from a CDN, so you need an internet connection.
- GitHub/GitLab Markdown viewers also render Mermaid in many cases.

